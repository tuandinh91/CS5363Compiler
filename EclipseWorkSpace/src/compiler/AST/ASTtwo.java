package compiler.AST;

import java.util.ArrayList;

import compiler.parserTree.PTreeNode;
import compiler.parserTree.ParsingTable;
import compiler.parserTree.TokenStream;

public class ASTtwo {
	/*
	private final ParsingTable grammar = new ParsingTable();
	private ArrayList<String> sequence = new ArrayList<String>();
	
	public ASTNode buildAST(TokenStream ts, String name) {
		ASTNode node = new ASTNode(name);
		
		if (name.startsWith("pro") ){ //non-terminals node
			node.getChildren().add(buildAST(ts, "decl list"));
		}
	}
	
*/
	
}
